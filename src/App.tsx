import React from 'react';
import styled from 'styled-components';

const AppWrapper = styled.div``;

const App: React.FC = () => {
  return (
    <AppWrapper>
      <h1>hello, walli</h1>
    </AppWrapper>
  );
};

export default App;
